<resources>
    <!-- Base application theme. -->
    <style name="Theme.LokaFreshInventory" parent="Theme.Material3.Light.NoActionBar">

        <item name="android:windowSplashScreenBackground">@color/theme_green</item>
        <item name="android:windowSplashScreenAnimatedIcon">@drawable/logo_white</item>

    </style>

    <style name="TopCornerChange" parent="ShapeAppearance.Material3.MediumComponent">
        <item name="cornerFamily">rounded</item>
        <item name="cornerSizeTopLeft">32dp</item>
        <item name="cornerSizeTopRight">32dp</item>
        <item name="cornerSizeBottomLeft">0dp</item>
        <item name="cornerSizeBottomRight">0dp</item>
    </style>

</resources>